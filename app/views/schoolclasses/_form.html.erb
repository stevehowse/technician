<%= form_for(@schoolclass) do |f| %>
  <% if @schoolclass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schoolclass.errors.count, "error") %> prohibited this schoolclass from being saved:</h2>

      <ul>
      <% @schoolclass.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :classname %><br />
    <%= f.text_field :classname %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
